export const environment = {
  production: true,
  api: {
    protocol: 'http',
    hostname: 'api.localhost',
    port: 80
  },
  auth: {
    protocol: 'http',
    hostname: 'auth.localhost',
    port: 80
  }
};
